

print( "Test." );

try
{
    var x = new MD5;

	for( var i = 1 ;; i++ )
	{
		System.sleep( 1 );
    	}
	}
catch( e )
{
	print( e );
	if ( e.left( 9 ) == "Terminate" )
	{
		print( "--> Stop with please received." );
		}
	}

print( "Done." );
